require './app.rb'
require 'sinatra/activerecord/rake'

namespace :wakame do
  desc "Shift your schedule to lower count of expired cards"
  task :comeback do
    throw StandardError.new('Moved to User class method')
  end
end
