ruby:
  words = Word.where(seq: @sentence.structure.map{|i|i['seq']}.compact).with_progresses(current_user)

div#sentence-quiz
  div.sentence-question
    span v-for="(word, wordIndex) of sentence" :class="word.seq ? 'word-question' : ''" @click="selectWord(word.seq)" = "{{word.text}}"
  p = "{{english}}"

== slim :word

javascript:
  var sentenceQuizApp = new Vue({
    el: '#sentence-quiz',
    data: {
      sentence: #{{@sentence.structure.to_json}},
      english: "#{{@sentence.english}}"
    },
    methods: {
      selectWord(seq){
        wordApp.getWordData(seq);
      }
    }
  });
