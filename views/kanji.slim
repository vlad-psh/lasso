div#kanji-card-app
  kanji :kanji="kanji" :editing="editing"

javascript:
  var app = new Vue({
    el: '#kanji-card-app',
    data: {
      kanji: #{{kanji_json(@kanji).to_json}},
      editing: false
    }
  })
