ruby:
  seq = params[:id] || @word_seq
  json = Collector.new(current_user, words: Word.where(seq: seq)).to_json

div#word-card-app
  vue-word :seq=seq :j="j" :editing="editing"

javascript:
  var app = new Vue({
    el: '#word-card-app',
    data: {
      j: #{{json}},
      editing: #{{user_settings('editing') || false}}
    }
  })
