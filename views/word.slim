- seq = params[:id] || @word_seq

div#word-card-app
  word :w="word" :editing="editing"

javascript:
  var app = new Vue({
    el: '#word-card-app',
    data: {
      word: #{{word_json(seq).to_json}},
      editing: false
    }
  })
